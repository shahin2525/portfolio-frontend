import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div>
      <Button className="text-red-400">Main Button</Button>
      <Button className="text-green-300">Main Button</Button>
      <Button className="text-yellow-400">Main Button</Button>
    </div>
  );
};

export default page;
